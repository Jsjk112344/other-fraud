---
phase: 04-cross-platform-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/stats.py
  - backend/agents/market_scan.py
  - backend/agents/cross_platform.py
  - backend/tests/test_stats.py
  - backend/tests/test_market_scan.py
  - backend/tests/test_cross_platform.py
autonomous: true
requirements:
  - PIPE-05
  - PIPE-06

must_haves:
  truths:
    - "compute_market_stats returns IQR-based outlier detection with median, percentile, and deviation for a list of prices"
    - "compute_market_stats gracefully returns nulls when fewer than 4 prices are provided"
    - "Market scan searches Carousell and Viagogo in parallel via asyncio.gather and merges results"
    - "Market scan falls back to MOCK_STEPS['check_market'] cached data on TinyFish failure"
    - "Cross-platform duplicate detection uses difflib.SequenceMatcher with 0.75 similarity threshold"
    - "Cross-platform search falls back to MOCK_STEPS['cross_platform'] cached data on failure"
  artifacts:
    - path: "backend/agents/stats.py"
      provides: "IQR-based statistical outlier detection"
      exports: ["compute_market_stats"]
      contains: "statistics.quantiles"
    - path: "backend/agents/market_scan.py"
      provides: "Parallel marketplace search for market rate calculation"
      exports: ["check_market_rates", "check_market_with_fallback"]
      contains: "asyncio.gather"
    - path: "backend/agents/cross_platform.py"
      provides: "Cross-platform duplicate detection with text similarity"
      exports: ["check_cross_platform", "cross_platform_with_fallback"]
      contains: "SequenceMatcher"
    - path: "backend/tests/test_stats.py"
      provides: "Unit tests for IQR stats"
      contains: "test_outlier_detection"
    - path: "backend/tests/test_market_scan.py"
      provides: "Unit tests for market scan with mocked TinyFish"
      contains: "test_fallback"
    - path: "backend/tests/test_cross_platform.py"
      provides: "Unit tests for duplicate detection and similarity"
      contains: "test_similarity"
  key_links:
    - from: "backend/agents/market_scan.py"
      to: "backend/agents/stats.py"
      via: "imports compute_market_stats"
      pattern: "from agents\\.stats import compute_market_stats"
    - from: "backend/agents/cross_platform.py"
      to: "difflib"
      via: "imports SequenceMatcher"
      pattern: "from difflib import SequenceMatcher"
    - from: "backend/agents/market_scan.py"
      to: "backend/mock/data.py"
      via: "fallback to MOCK_STEPS"
      pattern: "from mock\\.data import MOCK_STEPS"
---

<objective>
Build the three backend modules that power Phase 4's cross-platform intelligence: statistical outlier detection, market rate scanning across platforms, and cross-platform duplicate detection. All modules include fallback to cached mock data and are fully covered by unit tests.

Purpose: PIPE-05 and PIPE-06 require the agent to scan market rates and detect cross-platform duplicates. This plan builds the self-contained logic modules and their tests. Plan 02 wires them into the pipeline and updates the frontend.

Output: Three new agent modules (`stats.py`, `market_scan.py`, `cross_platform.py`), three test files, all tests green.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-platform-intelligence/04-RESEARCH.md
@.planning/phases/04-cross-platform-intelligence/04-VALIDATION.md

<interfaces>
<!-- Existing models and mock data the executor needs -->

From backend/models/events.py:
```python
class InvestigationEvent(BaseModel):
    step: str
    status: StepStatus
    data: Optional[dict] = None
```

From backend/models/enums.py:
```python
class StepStatus(str, Enum):
    PENDING = "pending"
    ACTIVE = "active"
    COMPLETE = "complete"
    ERROR = "error"
```

From backend/mock/data.py:
```python
MOCK_STEPS: dict[str, dict] = {
    "check_market": _raw["market"],      # keys: listings_scanned, platform_breakdown, average_price, median_price, min_price, max_price, this_listing_percentile, price_deviation_percent
    "cross_platform": _raw["cross_platform"],  # keys: duplicates_found, matches: [{platform, seller_name, listing_title, price, similarity_score}]
}
```

From backend/tests/conftest.py:
```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
```

Mock data shapes (from data/f1-gp-mock.json):
```json
{
  "market": {
    "listings_scanned": 24,
    "platform_breakdown": {"Carousell": 15, "Viagogo": 9},
    "average_price": 580.0,
    "median_price": 550.0,
    "min_price": 320.0,
    "max_price": 950.0,
    "this_listing_percentile": 3,
    "price_deviation_percent": -74.1
  },
  "cross_platform": {
    "duplicates_found": true,
    "matches": [{
      "platform": "Facebook Marketplace",
      "seller_name": "Fast Tickets SG",
      "listing_title": "F1 SG GP 2026 Pit Grandstand - CHEAP!!",
      "price": 160.0,
      "similarity_score": 0.92
    }]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stats module with IQR outlier detection and tests</name>
  <files>backend/agents/stats.py, backend/tests/test_stats.py, backend/agents/__init__.py</files>
  <read_first>
    - backend/tests/conftest.py
    - backend/mock/data.py
    - data/f1-gp-mock.json
    - .planning/phases/04-cross-platform-intelligence/04-RESEARCH.md (Pattern 2: IQR-Based Statistical Outlier Detection)
  </read_first>
  <behavior>
    - test_basic_stats: compute_market_stats([320,450,480,520,550,560,580,600,620,650,700,950], 150) returns average_price=580.0, median_price=555.0, min_price=320, max_price=950, is_outlier=True, outlier_direction="suspiciously_low"
    - test_percentile: compute_market_stats([100,200,300,400,500], 250) returns this_listing_percentile=40 (2 of 5 below)
    - test_deviation: compute_market_stats([100,200,300,400,500], 150) returns price_deviation_percent with negative value (below median)
    - test_outlier_high: compute_market_stats([100,200,300,400,500], 2000) returns is_outlier=True, outlier_direction="suspiciously_high"
    - test_not_outlier: compute_market_stats([100,200,300,400,500], 300) returns is_outlier=False, outlier_direction=None
    - test_insufficient_data: compute_market_stats([100, 200], 150) returns all None values except is_outlier=False
    - test_empty_prices: compute_market_stats([], 150) returns all None values except is_outlier=False
  </behavior>
  <action>
    1. Create `backend/agents/__init__.py` as empty file (if not exists) to make agents a package.

    2. Create `backend/tests/test_stats.py` with the 7 test cases listed in behavior. Import: `from agents.stats import compute_market_stats`. Each test calls compute_market_stats with specific inputs and asserts specific output keys and values.

    3. Run tests -- they MUST fail (RED).

    4. Create `backend/agents/stats.py` with function:
       ```python
       def compute_market_stats(prices: list[float], this_price: float) -> dict:
       ```
       Implementation:
       - If `len(prices) < 3`: return dict with all None values, `is_outlier=False`, `outlier_direction=None`
       - Compute `average_price = round(statistics.mean(prices), 2)`
       - Compute `median_price = round(statistics.median(prices), 2)`
       - Compute `min_price = sorted_prices[0]`, `max_price = sorted_prices[-1]`
       - Compute `this_listing_percentile = round((below_count / len(prices)) * 100)` where below_count = count of prices < this_price
       - Compute `price_deviation_percent = round(((this_price - med) / med) * 100, 1)`
       - Compute IQR: `q1, _q2, q3 = statistics.quantiles(prices, n=4)`, `iqr = q3 - q1`
       - `lower_fence = q1 - 1.5 * iqr`, `upper_fence = q3 + 1.5 * iqr`
       - `is_outlier = this_price < lower_fence or this_price > upper_fence`
       - `outlier_direction`: "suspiciously_low" if below lower_fence, "suspiciously_high" if above upper_fence, else None
       - Return dict with all keys matching the mock data shape: `average_price`, `median_price`, `min_price`, `max_price`, `this_listing_percentile`, `price_deviation_percent`, `is_outlier`, `outlier_direction`

    5. Run tests -- they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/tinyfish-project/backend && python -m pytest tests/test_stats.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/agents/stats.py contains `def compute_market_stats(`
    - backend/agents/stats.py contains `import statistics`
    - backend/agents/stats.py contains `statistics.quantiles(prices, n=4)`
    - backend/agents/stats.py contains `"suspiciously_low"` and `"suspiciously_high"`
    - backend/tests/test_stats.py contains `test_basic_stats`, `test_percentile`, `test_deviation`, `test_outlier_high`, `test_not_outlier`, `test_insufficient_data`, `test_empty_prices`
    - `cd backend && python -m pytest tests/test_stats.py -x -v` exits 0 with 7 tests passed
  </acceptance_criteria>
  <done>compute_market_stats correctly computes IQR-based outlier detection, percentile, deviation, and gracefully handles insufficient data. All 7 test cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Market scan and cross-platform agents with tests</name>
  <files>backend/agents/market_scan.py, backend/agents/cross_platform.py, backend/tests/test_market_scan.py, backend/tests/test_cross_platform.py</files>
  <read_first>
    - backend/agents/stats.py (just created in Task 1)
    - backend/mock/data.py
    - backend/tests/conftest.py
    - .planning/phases/04-cross-platform-intelligence/04-RESEARCH.md (Patterns 1, 3, and 4)
  </read_first>
  <behavior>
    Market scan tests (test_market_scan.py):
    - test_merge_platform_results: check_market_rates with mocked tinyfish_extract returns merged listings from both platforms, calls compute_market_stats
    - test_parallel_execution: Both platform searches are launched via asyncio.gather (mock both, verify both called)
    - test_fallback_on_failure: check_market_with_fallback returns (MOCK_STEPS["check_market"], False) when tinyfish_extract raises
    - test_fallback_on_insufficient: check_market_with_fallback returns fallback when listings_scanned < 3
    - test_live_success: check_market_with_fallback returns (result, True) when tinyfish_extract returns valid data with 3+ listings

    Cross-platform tests (test_cross_platform.py):
    - test_similarity_high: text_similarity("F1 SG GP 2026 Pit Grandstand", "F1 Singapore GP 2026 Pit Grandstand") >= 0.75
    - test_similarity_low: text_similarity("F1 tickets", "concert tickets cheap") < 0.75
    - test_similarity_exact: text_similarity("hello", "hello") == 1.0
    - test_duplicates_found: check_cross_platform with mocked search returns duplicates_found=True when similarity >= 0.75
    - test_no_duplicates: check_cross_platform with mocked search returns duplicates_found=False when no matches above threshold
    - test_fallback: cross_platform_with_fallback returns (MOCK_STEPS["cross_platform"], False) on failure
  </behavior>
  <action>
    1. Create `backend/tests/test_market_scan.py`:
       - Import `from agents.market_scan import check_market_rates, check_market_with_fallback`
       - Use `unittest.mock.AsyncMock` and `unittest.mock.patch` to mock `agents.market_scan.tinyfish_extract`
       - Mock returns: `[{"title": "F1 ticket", "price": 500, "seller": "user1"}, {"title": "F1 ticket 2", "price": 600, "seller": "user2"}]` for Carousell, similar for Viagogo
       - 5 test cases as listed in behavior above
       - All tests use `@pytest.mark.asyncio`

    2. Create `backend/tests/test_cross_platform.py`:
       - Import `from agents.cross_platform import text_similarity, check_cross_platform, cross_platform_with_fallback`
       - Mock `agents.cross_platform.tinyfish_extract` for async tests
       - Mock returns: `[{"title": "F1 SG GP 2026 Pit Grandstand - CHEAP!!", "seller_name": "Fast Tickets SG", "price": 160}]`
       - 6 test cases as listed in behavior above

    3. Run both test files -- MUST fail (RED).

    4. Create `backend/agents/market_scan.py`:
       - `async def tinyfish_extract(url, goal, stealth, proxy_country, timeout)` -- placeholder that will be replaced by Phase 2's real base agent. For now, raises NotImplementedError. Tests mock this function.
       - `async def scan_carousell_market(event_name: str, category: str) -> list[dict]`:
         - Build search URL: `f"https://www.carousell.sg/search/{quote(event_name)}"`
         - Call `tinyfish_extract` with url, goal asking for JSON array with title/price/seller, stealth=True, proxy_country="SG", timeout=15.0
         - Normalize result: if list return directly, if dict try result.get("listings") or result.get("results"), else empty list
       - `async def scan_viagogo_market(event_name: str) -> list[dict]`:
         - Call `tinyfish_extract` with url="https://www.viagogo.com/", goal asking to search for event_name and extract listings, stealth=True, proxy_country="SG", timeout=15.0
         - Same normalization as Carousell
       - `async def check_market_rates(event_name: str, category: str, this_listing_price: float) -> dict`:
         - `carousell_results, viagogo_results = await asyncio.gather(scan_carousell_market(event_name, category), scan_viagogo_market(event_name))`
         - Merge: `all_listings = carousell_results + viagogo_results`
         - Extract prices: `[float(l.get("price", 0)) for l in all_listings if l.get("price") and float(l.get("price", 0)) > 0]`
         - Call `compute_market_stats(prices, this_listing_price)`
         - Add `listings_scanned` and `platform_breakdown` keys
         - Return combined dict
       - `async def check_market_with_fallback(listing_data: dict) -> tuple[dict, bool]`:
         - Try `check_market_rates(event_name, category, price)` -- extract from listing_data
         - If result and `listings_scanned >= 3`: return `(result, True)`
         - On exception or insufficient data: return `(MOCK_STEPS["check_market"], False)`

    5. Create `backend/agents/cross_platform.py`:
       - `SIMILARITY_THRESHOLD = 0.75`
       - `def text_similarity(a: str, b: str) -> float`: uses `SequenceMatcher(None, a.lower().strip(), b.lower().strip()).ratio()`
       - `async def tinyfish_extract(url, goal, stealth, proxy_country, timeout)` -- same placeholder as market_scan
       - `async def search_platform_for_seller(platform_url: str, seller_name: str, stealth: bool = True) -> list[dict]`:
         - Call tinyfish_extract with goal to search for seller_name
         - Normalize result (same pattern)
       - `async def check_cross_platform(listing_title: str, seller_name: str, source_platform: str) -> dict`:
         - Build search_targets list based on source_platform (exclude source)
         - `asyncio.gather` all search tasks
         - For each result, compute `text_similarity` for title and seller
         - If max(title_sim, seller_sim) >= SIMILARITY_THRESHOLD, add to matches list
         - Return `{"duplicates_found": len(matches) > 0, "matches": matches}`
         - Each match: `{"platform", "seller_name", "listing_title", "price", "similarity_score": round(max(title_sim, seller_sim), 2)}`
       - `async def cross_platform_with_fallback(listing_data: dict) -> tuple[dict, bool]`:
         - Try check_cross_platform with listing_data fields
         - On success: return (result, True)
         - On exception: return (MOCK_STEPS["cross_platform"], False)

    6. Run all tests -- MUST pass (GREEN).

    Note: The `tinyfish_extract` placeholder in each module will be replaced by a real import from `agents.base` when Phase 2 is executed. For now, the function exists only so tests can mock it at the correct module path. Mark it with a `# TODO(phase-2): Replace with from agents.base import tinyfish_extract` comment.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/tinyfish-project/backend && python -m pytest tests/test_market_scan.py tests/test_cross_platform.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/agents/market_scan.py contains `async def check_market_rates(`
    - backend/agents/market_scan.py contains `async def check_market_with_fallback(`
    - backend/agents/market_scan.py contains `asyncio.gather`
    - backend/agents/market_scan.py contains `from agents.stats import compute_market_stats`
    - backend/agents/market_scan.py contains `MOCK_STEPS`
    - backend/agents/cross_platform.py contains `SIMILARITY_THRESHOLD = 0.75`
    - backend/agents/cross_platform.py contains `SequenceMatcher`
    - backend/agents/cross_platform.py contains `async def check_cross_platform(`
    - backend/agents/cross_platform.py contains `async def cross_platform_with_fallback(`
    - backend/tests/test_market_scan.py contains `test_merge_platform_results`, `test_fallback_on_failure`, `test_live_success`
    - backend/tests/test_cross_platform.py contains `test_similarity_high`, `test_similarity_low`, `test_duplicates_found`, `test_fallback`
    - `cd backend && python -m pytest tests/test_market_scan.py tests/test_cross_platform.py tests/test_stats.py -x -v` exits 0 with all tests passed
  </acceptance_criteria>
  <done>Market scan agent searches two platforms in parallel with fallback. Cross-platform agent detects duplicates via text similarity with fallback. All 11+ test cases pass alongside the 7 stats tests from Task 1.</done>
</task>

</tasks>

<verification>
Full backend test suite passes:
```bash
cd backend && python -m pytest tests/ -x -v
```
All 18+ new tests pass (7 stats + 5 market + 6 cross-platform).
Existing Phase 1 tests still pass.
</verification>

<success_criteria>
- backend/agents/stats.py exists with compute_market_stats using IQR detection
- backend/agents/market_scan.py exists with parallel Carousell+Viagogo search and fallback
- backend/agents/cross_platform.py exists with similarity-based duplicate detection and fallback
- All three test files exist and pass
- No new external dependencies added (all stdlib + existing packages)
- Output data shapes match mock data structure from data/f1-gp-mock.json
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-platform-intelligence/04-01-SUMMARY.md`
</output>
